//using forloop
#include<stdio.h>
int  main()
{
	float ;
	int a,sum,average,number;
	for(a=1;a<=200;a+=5)//a++ not a+1//their is nn (;) for for at the end of the code
	{
		printf("enter any number:\n");
		scanf("%d",&number);
		sum+=number;  //sum=sum+number
		average=sum/7;
		printf("The average of the numbers are %.2f",average);
	}
	
	
}				
